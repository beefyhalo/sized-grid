-- sized-grid-kb38 spike. Two packages: this throwaway one, and the real
-- library, so the control (the library's own 'stencilFor') and the
-- experiments are the same binary, built by the same compiler, measured back
-- to back.
packages:
  .
  ../..
